import { useMithril } from "./MithrilContext";

export default function Stage2Content() {
  // Example usage of context - uncomment and use as needed:
  // const { uploadedFiles, getStageResult, setStageResult, goToNextStage } = useMithril();

  return (
    <div>
      <h2 className="text-2xl font-bold text-center mb-4">Stage 2</h2>
      <p className="text-gray-500 dark:text-gray-400 text-center">
        Stage 2 Content - Placeholder
      </p>
    </div>
  );
}
